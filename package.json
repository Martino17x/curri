{
  "name": "currito-monorepo",
  "private": true,
  "version": "0.2.0",
  "description": "Currito — monorepo: editor de currículums ATS-friendly (React + Vite) y landing page (Astro).",
  "license": "MIT",
  "author": "Martino Costigliolo",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "smoke": "pnpm --filter currito smoke",
    "check": "pnpm typecheck && pnpm build && pnpm smoke"
  },
  "devDependencies": {
    "turbo": "^2.5.6"
  }
}
