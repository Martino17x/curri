import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // TODO: al desplegar la landing, reemplazar por el dominio real (ej. https://currito.app)
  site: 'https://curri.vercel.app',
});
